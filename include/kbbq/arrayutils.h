#ifndef KBBQ_ARRAY_UTILS_H
#define KBBQ_ARRAY_UTILS_H
// #include <stdbool.h>

/* Increment indices specified in array i. Beware bounds are not checked! */
static void increment_at_i(int a[], unsigned int i[], unsigned int n);

/* Increments indices using a T/F array. Both arrays should be len n. */
// static void increment_at_b(int a[], bool i[], unsigned int n);

#endif
